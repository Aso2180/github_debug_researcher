# Expressのポート
PORT=3000

# collector側と同じDB。開発時は collector/ からの相対パスで指定
DATABASE_URL=sqlite:///../collector/tech_stack.db

# Basic認証の資格情報(本番では強力なパスワードに変更すること)
DASHBOARD_USER=admin
DASHBOARD_PASSWORD=changeme
